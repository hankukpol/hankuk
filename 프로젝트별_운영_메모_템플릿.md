# 프로젝트별 운영 메모 템플릿

> 이 문서는 각 프로젝트의 운영 정보를 같은 형식으로 남기기 위한 템플릿입니다.
> 새 앱이든 기존 앱이든, 서비스 경계와 tenant 구조가 드러나게 기록하는 것이 목표입니다.

---

## 1. 기본 정보

- 프로젝트명:
- 폴더 경로:
- 한 줄 설명:
- 이 프로젝트는:
  - 새 서비스 앱 / 기존 서비스 앱
- 현재 상태:
  - 개발 중 / 운영 중 / 마이그레이션 대기 / 중단 예정

---

## 2. 서비스 경계

- 이 앱이 담당하는 서비스:
- 같은 저장소 안에서 관련 있는 다른 앱:
- 이 앱 안의 tenant/division/workspace 종류:
- tenant를 새 앱으로 분리하지 않는 이유:

---

## 3. 기술 스택

- Framework:
- Language:
- Package manager:
- Node version:
- Next.js version:
- React version:
- ORM:
- DB:
- Styling:

---

## 4. 실행 방법

- install:
- dev:
- build:
- start:
- lint:
- typecheck:
- test:

---

## 5. 라우팅 / tenant 구조

- 서비스용 subdomain alias:
- Production URL:
- tenant 식별 방식:
  - path slug / hostname / runtime lookup
- tenant slug 예시:
- 기본 라우트 예시:

예:

- `study.hankukpol.co.kr/police/...`
- `study.hankukpol.co.kr/fire/...`

---

## 6. 환경변수

- 사용 env 파일:
- 공유 Supabase 값 사용 여부:
- 앱 전용 env 목록:
- tenant를 env로 고정하는 값이 있는지:
- 있으면 제거/전환 계획:

---

## 7. 데이터베이스 / Supabase

- Supabase project ref:
- app schema 이름:
- 공통 `public` 의존 테이블:
- tenant 구분 키:
  - `division_id` / `workspace_id` / `tenant_slug`
- tenant 경계가 필요한 주요 테이블:
- Prisma 사용 여부:
- Prisma schema 위치:

---

## 8. 인증 / 권한

- 로그인 방식:
- 공통 인증 공유 여부:
- tenant 문맥이 세션에 들어가는지:
- tenant별 권한 차이:
- 보호해야 하는 라우트:

---

## 9. 주요 기능

- 핵심 화면 1:
- 핵심 화면 2:
- 핵심 화면 3:
- 핵심 API 1:
- 핵심 API 2:

---

## 10. Vercel / 배포

- Vercel 프로젝트명:
- Root Directory:
- Preview URL:
- Production URL:
- subdomain alias:
- tenant 때문에 추가 Vercel 프로젝트가 필요한지:
  - 기본값은 아니어야 함

---

## 11. tenant 운영 메모

- 현재 지원 tenant/division:
- 추가 예정 tenant/division:
- tenant별 차이:
  - 브랜드
  - 문구
  - 권한
  - 제한된 규칙
- 공통 로직과 tenant 전용 로직 경계:

---

## 12. 자주 확인할 것

- tenant 필터 빠진 쿼리가 없는지
- auth/session이 tenant 경계를 지키는지
- build-time tenant env 의존이 남아 있는지
- 같은 앱에서 여러 tenant를 동시에 검증할 수 있는지

---

## 13. 변경 이력

- YYYY-MM-DD:
  - 변경 내용:
  - 영향 범위:

- YYYY-MM-DD:
  - 변경 내용:
  - 영향 범위:
