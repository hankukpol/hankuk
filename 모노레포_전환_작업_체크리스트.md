# 모노레포 전환 작업 체크리스트

> 이 문서는 `D:\hankuk`를 목표 모노레포 구조로 전환할 때 따라갈 체크리스트입니다.
> 기준 규칙은 항상 `D:\hankuk\AGENTS.md`를 따릅니다.

---

## 1. 목표 다시 확인

전환 목표:

- 루트 Git 저장소 1개
- GitHub 저장소 1개
- Turborepo + pnpm workspace
- 앱은 `apps/`
- 공통 코드는 `packages/`
- 앱별 새 Vercel 프로젝트 3개
- 단일 Supabase 프로젝트 + 앱별 schema 분리
- 같은 앱 안의 경찰/소방/지점은 runtime tenant 처리

---

## 2. 가장 먼저 결정해야 할 것

- [ ] 이 기능이 새 앱인지, 기존 앱의 tenant 확장인지 판단
- [ ] 앱 경계와 tenant 경계를 분리해서 생각하기
- [ ] `study-hall` 방식의 runtime division 처리를 기준 패턴으로 삼을지 확인

---

## 3. 하면 안 되는 것

- [ ] 경찰/소방 차이만 있다고 새 앱을 만드는 것
- [ ] 지점이 늘어날 때마다 새 Vercel 프로젝트를 만드는 것
- [ ] tenant마다 새 Supabase 프로젝트를 만드는 것
- [ ] build-time env 하나로 tenant를 영구 고정하는 것

---

## 4. 현재 상태 파악

- [ ] `score-predict`, `study-hall`, `interview-pass` 현재 구조 확인
- [ ] 각 앱의 Next.js / React / Prisma / Supabase 버전 확인
- [ ] 각 앱의 Vercel 연결 상태 확인
- [ ] 각 앱의 tenant 처리 방식 확인
- [ ] `score-predict`, `interview-pass`의 `NEXT_PUBLIC_TENANT_TYPE` 사용 범위 확인
- [ ] `study-hall`의 `[division]` 처리 구조 확인

---

## 5. 루트 Git / GitHub 준비

- [ ] `interview-pass/.git` 백업 여부 결정
- [ ] app-level `.git` 제거 계획 확정
- [ ] 루트 Git 초기화
- [ ] 루트 GitHub 저장소 연결

---

## 6. 루트 모노레포 파일 생성

- [ ] 루트 `package.json`
- [ ] 루트 `pnpm-workspace.yaml`
- [ ] 루트 `turbo.json`
- [ ] 루트 `.nvmrc`
- [ ] `engines.node` 정의

---

## 7. 앱 이동

- [ ] `apps/` 생성
- [ ] `packages/` 생성
- [ ] `score-predict` -> `apps/score-predict`
- [ ] `study-hall` -> `apps/study-hall`
- [ ] `interview-pass` -> `apps/interview-pass`

---

## 8. npm -> pnpm 전환

- [ ] 각 앱이 기존 npm 상태에서 build 되는지 확인
- [ ] 각 앱의 `package-lock.json` 제거
- [ ] 루트 `pnpm install`
- [ ] 루트 `pnpm-lock.yaml` 생성 확인

---

## 9. 통합 Supabase 준비

- [ ] 새 통합 Supabase 프로젝트 생성
- [ ] `public` schema 설계
- [ ] `score_predict` schema 설계
- [ ] `study_hall` schema 설계
- [ ] `interview` schema 설계

### tenant 모델

- [ ] 각 앱 안에서 tenant를 어떤 키로 구분할지 결정
  - `division_id`
  - `workspace_id`
  - `tenant_slug`
- [ ] tenant-aware 앱의 모든 쿼리에 경계 필터가 들어갈 수 있게 모델 설계
- [ ] 공통 인증과 tenant 문맥이 함께 동작하는지 설계

---

## 10. 버전 정책 정리

- [ ] 단일 Node 버전 결정
- [ ] Next.js major 정렬
- [ ] React major 정렬
- [ ] Prisma major 정렬 여부 판단
- [ ] `@supabase/supabase-js` 정렬 여부 판단
- [ ] `@supabase/ssr` 정렬 여부 판단

---

## 11. tenant 구조 정리

### `study-hall`

- [ ] 현재 `[division]` 구조를 기준 패턴으로 문서화
- [ ] 권한, 미들웨어, API, 쿼리 필터가 division 기준으로 일관적인지 확인

### `score-predict`

- [ ] `NEXT_PUBLIC_TENANT_TYPE` 의존 범위 확인
- [ ] runtime tenant 구조로 옮길 수 있는 부분 분리
- [ ] path/slug 또는 request 기준 tenant 처리 계획 수립

### `interview-pass`

- [ ] `NEXT_PUBLIC_TENANT_TYPE` 의존 범위 확인
- [ ] runtime tenant 구조로 옮길 수 있는 부분 분리
- [ ] 라벨/브랜딩/필드 차이를 config로 옮길 계획 수립

---

## 12. 공유 코드 분리

### 먼저 분리 가능한 것

- [ ] utils
- [ ] config
- [ ] types
- [ ] validation schemas
- [ ] constants

### 버전 정렬 후에만 가능한 것

- [ ] `@hankuk/ui`
- [ ] `@hankuk/auth`
- [ ] `@hankuk/db`

### tenant 관련 주의

- [ ] tenant 전용 코드가 `packages/`로 새지 않는지 확인
- [ ] 앱 전용 tenant 규칙은 앱 안에 유지

---

## 13. 환경변수 구조 정리

- [ ] 루트 `.env`는 모노레포 툴링 + 공유 Supabase 값만 사용
- [ ] 각 앱 `.env.local` 유지
- [ ] 새 tenant 추가 시 build-time env 하나로 tenant를 고정하지 않도록 점검
- [ ] 공통 인증/세션 공유에 필요한 env 정리

---

## 14. Vercel 전환

- [ ] 새 Vercel 프로젝트 3개 생성
- [ ] 각 앱 Root Directory를 `apps/<app-name>`으로 지정
- [ ] 앱별 subdomain alias 연결

### tenant 관점

- [ ] tenant 때문에 Vercel 프로젝트를 추가로 만들지 않는지 확인
- [ ] 같은 앱이 여러 tenant를 runtime에 처리하는지 확인

---

## 15. 검증

### 빌드 / 테스트

- [ ] 루트 `pnpm install`
- [ ] 루트 `turbo run build`
- [ ] 영향받는 앱별 lint/typecheck/build

### tenant 검증

- [ ] 같은 앱에서 둘 이상의 tenant가 동시에 동작하는지 확인
- [ ] auth/session이 tenant 경계를 제대로 따르는지 확인
- [ ] API와 DB 쿼리에 tenant 필터가 빠지지 않았는지 확인

---

## 16. 정리

30일 안정 운영 후에만 진행:

- [ ] 기존 Vercel 프로젝트 삭제
- [ ] 기존 Supabase 프로젝트 삭제

---

## 17. 전환 완료 조건

- [ ] 루트 Git 저장소 1개
- [ ] `apps/` 구조 정착
- [ ] `packages/` 구조 정착
- [ ] 단일 Supabase 프로젝트 + 앱별 schema 분리 완료
- [ ] 앱별 Vercel 프로젝트 3개 정상 운영
- [ ] 같은 앱 안 multi-tenant 구조 정상 동작
- [ ] `study-hall` 방식의 runtime tenant 원칙이 표준으로 정착
