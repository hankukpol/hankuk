# UIUX 리뷰 후속 수정 — 2026-09-05

범위: 리뷰 R01~R09에 대한 로컬 수정·검증. 기존 작업 트리의 다른 등록/결제/자료 변경을 보존했다. 운영 DB, Git 커밋·푸시, Vercel 배포는 하지 않았다.

| 항목 | 처리 | 확인 근거 |
| --- | --- | --- |
| R01 등록 초안 소실 | 병행 작업에서 이미 추가된 미저장 경고를 보존·재검증 | 이름 입력 → 닫기 → 계속 작성 시 초안 유지, Escape → 폐기 확인. 등록 워크플로 테스트 통과 |
| R02 정산 합계 가독성 | 평면 페이지의 합계를 세로 라벨/값 + 24px 간격 + 항목별 직선으로 분리. 모달 내부 strip은 기존 유지 | 1280px에서 여섯 합계가 299px씩 3열×2행. 이전 인접 항목 간격 1px → 24px |
| R03 잘못된 grid 문법 | 일일·월별·가져오기·강좌 설정·좌석 과목 폼의 쉼표 트랙 구분 수정. 입력 트랙 minmax(0,1fr). 반복 필드의 헤더/행 폭 통일 | 일일 5열, 월별 4열, 월별 본문 2열, 가져오기·좌석 폼 실제 computed grid 확인 |
| R04 넓은 명단 | 기본 6열, 상세 열 토글. 이름 열·헤더 고정, 별도 메모 유지. 숨긴 열의 정렬도 상단 설명으로 확인 | 1280px 기본 표 930px/프레임 945px. 상세 표 scrollLeft 385px에서도 이름 x=프레임 x=288px. scrollTop 465px에서도 헤더 y=프레임 y |
| R05 작은 화면 밀도 | 사이드바 기준 1024px. 접힌 관리자 메뉴, 모바일 5상태 선택창, 한 줄 서브 탭 | 768px 본문 449→705px. 390px 첫 학생 y≈903→643px. 메뉴 Escape 포커스 복귀, 외부 클릭·링크 이동 닫기 테스트 |
| R06 설정 라벨 | 보이는 라벨 24개와 입력을 htmlFor/id로 연결. 새/기존 과목·반복 정보 필드에도 접근성 이름 | 기본 13개 입력의 실제 labels 확인, 위치 인증 8개 컨트롤 labels 확인. SSR 회귀 테스트에 반복 필드 포함 |
| R07 위험 버튼 대비 | --admin-danger를 #b91c1c로 변경 | 활성 메모 삭제가 흰 배경/185,28,28로 표시. 대비 약 6.47:1. 저장·취소·닫기 유지 |
| R08 검색 포커스 외곽선 | input-group은 기존 테두리만 accent 강조 | focus-within outline-style:none, box-shadow:none. 등록 drawer 모서리 0px 유지 |
| R09 경고 후속 동작 | 학생 인증은 관리 화면 링크, 운영 경고는 해당 강좌 필터 + 전체 보기 복귀 | 출석 경고 클릭 시 8개→관련 1개, 전체 보기 8개 복원. 포커스가 강좌 제목으로 이동. GET만 발생하는 회귀 테스트 |

## 검증

- 관리자 회귀 테스트 **120개 통과**, 실패·생략 0개. 기존 무료/혼합 강좌, 수납 입력, 초안·pending 보호, 정산/자료 안전성 테스트도 포함한다.
- `pnpm exec tsc --noEmit --incremental false` 통과.
- `pnpm verify:admin-presentation`: 76개 소스 검사 통과. 76개 화면을 전부 브라우저 확인했다는 뜻은 아니다.
- 실제 Chrome 로컬 명단: 390×844, 768×1024, 1280×900, 1760×1000. 검색 기준선, 집계 간격, 상태 필터 오른쪽 정렬, 겹침·넘침 검사 통과.
- 로컬 대시보드, 일일·월별 정산, 엑셀 가져오기, 설정, 좌석 과목 관리, 등록·메모 모달 직접 확인. 등록은 검증용 이름만 입력 후 폐기했고 저장하지 않았다. 메모는 기존 내용 보기·취소만 실행했다.
- 명단 수치: `.superloopy/sessions/20260905-uiux-fixes/evidence/roster-geometry.json`; 검사: `node scripts/verify-roster-toolbar-geometry.mjs <해당 JSON>`.
- 테스트 원문: `.superloopy/sessions/20260905-uiux-fixes/evidence/G001-C002-capture.txt`.
- DS 검사에서 미선언 색은 없었다. 외부 검사기가 `border-bottom: 2px`를 spacing의 `bottom`으로 오인한 기존 1건은 의도된 서브 탭 선택선이므로 변경하지 않았다.

## 한계

- ESLint는 저장소에 v9 설정 파일이 없어 실행 단계에서 중단되었다. 이를 통과로 기록하지 않는다.
- 운영 결제 저장, 실제 학생 폰/스크린리더, 가상 키보드, 프로덕션 성능·Lighthouse는 검증하지 않았다. 배포 승인과는 별개다.
- 다른 작업도 진행 중인 작업 트리다. 이 문서는 이번 수정과 위 검증 시점의 결과이며 전체 변경의 배포 적합성을 보증하지 않는다.
